# 异常信息读取对齐审计

## 结论

本次以截图规格为唯一基准核对 `bat_record` 异常信息读取与 APP 展示语义。

- 过压异常：基本对齐，保留现有 20B `BatteryExceptionRecord_t` 协议结构。
- 温度异常：原实现不对齐，已按截图规格修正固件逻辑。

## 对比表

| 项目 | 固件原实现 | 截图规格 | 结论 |
| --- | --- | --- | --- |
| 电芯过压阈值 | `OVER_VOLTAGE_THRESHOLD = 4450mV` | 每级电池充电上限电压 `4.45V` | 已对齐 |
| 过压记录内容 | 记录时间、异常类型、电芯号、最高单节电压、总压、`record_id` | 记录异常值和产生时间，APP 显示异常信息及时间 | 已对齐 |
| 过压协议格式 | `error_type=0x01`，`sub_type=电芯号`，`data_low=max_voltage`，`data_high=total_voltage` | APP 读取异常日志字段 | 已对齐 |
| 温度异常阈值 | 仅按 `>60.0C` 判定 | 充电 `60C`，放电 `65C` | 不对齐，已修正 |
| 温度模式区分 | 未区分充电/放电阈值 | 软件逻辑先判断电芯 NTC 温度是否在进行充电或者放电 | 不对齐，已修正 |
| 待机态处理 | 待机高温也可能进入异常记录流程 | 待机状态下异常温度不算异常状态 | 不对齐，已修正 |
| 温度子类型语义 | 直接写入 `woke_mode`，实际值为 `1/2` | APP/Windows 将温度 `sub_type` 解释为 `0=充电, 1=放电` | 不对齐，已修正 |
| 温度协议格式 | `error_type=0x02`，`data_low=max_temperature(0.1C)`，`data_high=0` | APP 显示异常温度和时间 | 已对齐 |
| 欠温日志 | 当前未主动扩展 `0x03` 欠温逻辑 | 截图未要求新增欠温展示 | 保持不变 |

## 已实施改动

### 固件

- 温度异常改为按工作模式判断：
  - 充电态 `> 60.0C` 记录过温
  - 放电态 `> 65.0C` 记录过温
  - 待机态不记录温度异常
- 温度异常 `sub_type` 固定输出协议语义：
  - `0 = 充电`
  - `1 = 放电`
- 保持 `BatteryExceptionRecord_t` 结构不变，避免影响 WB7720 和 APP 读取格式。

### 测试

- 新增过压 `4450mV` 边界测试。
- 新增充电 `60.0C` 与放电 `65.0C` 的温度边界测试。
- 新增待机态高温不产生日志测试。
- 新增温度异常在 1 小时窗口内沿用同一 `record_id` 只更新峰值的测试。

## 当前对齐后的异常语义

### 过压异常

- `error_type = 0x01`
- `sub_type = 电芯号`
- `data_low = 单节最高电压(mV)`
- `data_high = 总压(mV)`

### 温度异常

- `error_type = 0x02`
- `sub_type = 0(充电) / 1(放电)`
- `data_low = 最高温度(0.1C)`
- `data_high = 0`

## 备注

- 本轮只处理异常信息读取与展示语义对齐，不涉及 UI 样式调整。
- 仓库内旧文档若仍写温度 `sub_type = 0/1` 之外的含义，应以后续文档同步为准。
