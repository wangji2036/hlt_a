# 新国标功能开发指南（内部版）

**项目**: ARUN IP162N 15W PowerBank
**MCU**: NU17112 (CK802 big-endian @ 36 MHz, 120KB Flash, 8KB SRAM)
**充电 IC**: NU6805 (双节串联)
**版本**: v1.0
**受众**: 固件工程师（内部）

---

## 目录

1. [概述与特性依赖图](#1-概述与特性依赖图)
2. [Flash 布局](#2-flash-布局)
3. [数据结构参考](#3-数据结构参考)
4. [特性详解](#4-特性详解)
   - 4.1 电池循环充电次数自动统计
   - 4.2 循环次数驱动的 CV 降压分级
   - 4.3 SoH / Rdc 估算
   - 4.4 异常日志 (bat_record) — OV/OT 记录
   - 4.5 动态阻抗检测 (dyn_imp)
   - 4.6 RTC 时间戳
   - 4.7 ProductInfo 产品信息持久化
   - 4.8 USB 监测子系统（三击激活 + 遥测上报）
5. [特性间依赖关系](#5-特性间依赖关系)
6. [开启/关闭方法 — config.h 宏说明](#6-开启关闭方法--configh-宏说明)
7. [测试与验证](#7-测试与验证)
8. [已知限制与注意事项](#8-已知限制与注意事项)

---

## 1. 概述与特性依赖图

### 1.1 新国标改动全貌

本批改动针对「新国标」电池管理合规要求，所有特性受顶层宏 `CONFIG_NEW_CCC_LOG_ENABLE = 1` 门控。涉及的源文件跨越 5 个功能域：

| # | 特性名称 | 核心文件 | 独立宏 |
|---|----------|---------|--------|
| 1 | 电池循环次数统计 | `power/bat.c`, `power/bat.h` | `CONFIG_BAT_CYCLE_COUNT_ENABLE` |
| 2 | CV 降压分级 | `app/config.h`, `fml/_fml.c` | `CONFIG_CYCLE_CV_REDUCTION_ENABLE` |
| 3 | SoH / Rdc 估算 | `fml/_fml.c` | (依赖特性 1) |
| 4 | 异常日志 OV/OT | `app/bat_record.c/h` | `CONFIG_NEW_CCC_LOG_ENABLE` |
| 5 | 动态阻抗检测 | `app/dyn_imp.c/h` | `CONFIG_DYNAMIC_IMPEDANCE_ENABLE` |
| 6 | RTC 时间戳 | `fml/g_data.c/h` | `CONFIG_RTC_USE_CUSTOM_TIME` |
| 7 | ProductInfo 持久化 | `app/product_info.c/h` | `CONFIG_NEW_CCC_LOG_ENABLE` |
| 8 | USB 监测子系统（三击 + 遥测）| `app/usb_bridge.c/h`, `fml/_fml.c`, `libpowerbank/typec.c` | `CONFIG_USB_BRIDGE_ENABLE`, `CONFIG_TRIPLE_CLICK_COMM_ENABLE` |

### 1.2 特性依赖图

```
CONFIG_BAT_CYCLE_COUNT_ENABLE
        │
        ▼
  ┌─────────────────┐
  │  特性1: 循环计数  │──────────────────────────────┐
  │  Battery_cycle_ │                              │
  │  count (gd→)   │                              │
  └────────┬────────┘                              │
           │                                       │
           ▼                                       ▼
  ┌─────────────────┐                   ┌──────────────────┐
  │  特性2: CV降压   │                   │  特性3: SoH/Rdc   │
  │  cycle_cv_      │                   │  Bat_SoH(int8_t) │
  │  offset_mv(gd→) │                   │  Bat_Rdc(uint8_t)│
  └─────────────────┘                   └────────┬─────────┘
                                                  │
CONFIG_DYNAMIC_IMPEDANCE_ENABLE                   │ (via USB Bridge)
        │                                         │
        ▼                                         ▼
  ┌─────────────────┐     IR补偿     ┌──────────────────────┐
  │  特性5: dyn_imp  │──────────────▶│  特性4: bat_record    │
  │  R_pos/R_neg    │               │  OV/OT 异常日志       │
  └─────────────────┘               └──────────┬───────────┘
                                                │
CONFIG_RTC_USE_CUSTOM_TIME                      │ (timestamp)
        │                                       │
        ▼                                       │
  ┌─────────────────┐                           │
  │  特性6: RTC时间戳 │──────────────────────────┘
  │  Bat_RTC_Seconds│
  └─────────────────┘

CONFIG_NEW_CCC_LOG_ENABLE (全局门控)
        │
        ▼
  ┌─────────────────┐
  │  特性7: ProductInfo│
  │  Flash 0x1800   │
  └─────────────────┘

CONFIG_USB_BRIDGE_ENABLE
        │
        ▼
  ┌──────────────────────────────────────┐
  │  特性8: USB Bridge 遥测              │
  │  (特性1/3/4/7 数据→WB7720→PC HID)   │
  └──────────────────────────────────────┘
```

---

## 2. Flash 布局

### 2.1 完整地址表

```
Flash 地址空间 (NU17112, 120KB)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  地址范围         大小    用途
  0x0000~0x13FF   5KB     LDROM (Bootloader, 只读)
  0x1400~0x15FF   512B    异常日志 (bat_record, AP_CFG_ROM_ADDR_LOG=0x1400)
  0x1600~0x17FF   512B    Q/freq 校准值 + Gauge 参数 (AP_CFG_ROM_ADDR_BASE=0x1600)
  0x1800~0x19FF   512B    ProductInfo (AP_CFG_ROM_ADDR_PRO_INFO=0x1800)  ← 特性7
  0x1A00~0x1BFF   512B    CycleCount (ADDR_CYCLE_COUNT_PAGE=0x1A00)      ← 特性1
  0x1C00~0x1DFF   (保留/扩展)
  0x2000~...             APROM 程序代码
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

> **注意**: NU17112 Data Flash 页大小为 512B，每次写入前必须整页擦除。所有 Flash 操作都经过 `hal_fmc_erase_page()` + 字写入（4B 对齐）。

### 2.2 各模块 Flash 页详解

#### 2.2.1 异常日志页 (0x1400~0x15FF)

```
偏移   大小   字段名                   说明
0x00   4B    magic                    版本标识: 'BAT6' = 0x42415436
0x04   4B    exception_counter        历史异常总计数 (0~255, 单调递增)
0x08   4B    write_ptr                环形缓冲写指针 (0~4)
0x0C   4B    padding1                 对齐填充
0x10   100B  records[0..4]            5条异常记录，每条 20B
            (5 × BatteryExceptionRecord_t)
0x74   2B    checksum                 简单加法校验和
0x76   ...   (剩余填充)
─────────────────────────────────────────────────────────
MAGIC V5 (old): 0x42415430 ('BAT0'), V6 (current): 0x42415436 ('BAT6')
```

#### 2.2.2 ProductInfo 页 (0x1800~0x19FF)

```
偏移   大小   字段名                  说明
0x00   20B   manufacturer_name       制造商名称 (ASCII, null terminated)
0x14   20B   model_name              产品型号
0x28   20B   battery_mfr             电芯制造商
0x3C   20B   battery_model           电芯型号
0x50   20B   battery_prod_date       电芯生产日期
0x64   2B    version                 版本字: 0x0001
0x66   ...   (剩余填充)
```

#### 2.2.3 CycleCount 页 (0x1A00~0x1BFF)

```
偏移   大小   字段名                  说明
0x00   8B    CycleCountStorage_t     见 §3 数据结构
0x08   ...   (剩余未用)
```

---

## 3. 数据结构参考

### 3.1 CycleCountStorage_t — Flash 持久化 (8B)

```c
/* power/bat.h */
typedef struct {
    uint8_t  cycle_count;            /* [0] Battery_cycle_count 镜像 (0~255) */
    uint8_t  soc_prev_dischg;        /* [1] 上次放电 SOC 基线 (0xFF=未初始化) */
    uint16_t soc_accum_hundredths;   /* [2-3] SOC 累积器, 单位 0.01%, 范围 0~9999 */
    uint8_t  checksum;               /* [4] XOR(bytes[0..3]) 校验 */
    uint8_t  checksum_inv;           /* [5] ~checksum (可检测全FF擦除态) */
    uint16_t reserved;               /* [6-7] 对齐填充 */
} CycleCountStorage_t;               /* 总计: 8 bytes = 2 Flash words */
```

**校验说明**: 双重校验 (checksum + checksum_inv) 可区分"全0xFF已擦除页"和"有效数据"——如果 checksum=0xFF 且 checksum_inv=0x00，则视为有效；如果两者均为 0xFF，则视为未初始化。

### 3.2 TimeStamp_t — 时间戳 (8B)

```c
/* fml/g_data.h */
typedef struct {
    uint16_t year;      /* [0-1] 年份, 2026~2099 */
    uint8_t  month;     /* [2]   月份, 1~12 */
    uint8_t  day;       /* [3]   日,   1~31 */
    uint8_t  hour;      /* [4]   时,   0~23 */
    uint8_t  minute;    /* [5]   分,   0~59 */
    uint8_t  second;    /* [6]   秒,   0~59 */
    uint8_t  reserved;  /* [7]   对齐保留 */
} TimeStamp_t;          /* 总计: 8 bytes */
```

### 3.3 BatteryExceptionRecord_t — 异常记录 (20B)

```c
/* fml/g_data.h */
typedef struct {
    TimeStamp_t timestamp;      /* [0-7]  8B: 记录时间戳 */
    uint8_t  error_type;        /* [8]    1B: 0x01=OV, 0x02=OT, 0x03=UT */
    uint8_t  sub_type;          /* [9]    1B: OV=cell编号(1/2); TEMP=charge_state(0=CHG,1=DCHG) */
    union {
        struct {
            uint16_t max_voltage;    /* [10-11] 触发时单节最大电压 (mV) */
            uint16_t total_voltage;  /* [12-13] 触发时总电压 (mV) */
            uint16_t pad;            /* [14-15] 填充 */
        } ov_data;
        struct {
            int16_t  max_temperature; /* [10-11] 触发时最高温度 (0.1°C) */
            uint16_t reserved;        /* [12-13] 保留 */
            uint16_t pad;             /* [14-15] 填充 */
        } temp_data;
        uint8_t raw_data[6];
    } data;                     /* [10-15] 6B: 联合体数据区 */
    uint32_t record_id;         /* [16-19] 4B: 单调递增序号 */
} BatteryExceptionRecord_t;     /* 总计: 20 bytes */
```

> **Big-endian 提醒**: CK802 为大端序。多字节字段在 Flash 中以大端格式存储，PC 读取后需字节序转换。

### 3.4 ExceptionCache_t — RAM 追踪缓存 (~16B)

```c
/* fml/g_data.h, 存放在 ap->exception_cache */
typedef struct {
    uint8_t  status_flags;               /* 位域标志 (详见下方 bitfield 说明) */
    uint8_t  padding1;
    uint16_t cell1_max_voltage;          /* Cell1 实时最大电压 (mV) */
    uint16_t cell2_max_voltage;          /* Cell2 实时最大电压 (mV) */
    int16_t  max_temperature;            /* 实时最高温度 (0.1°C) */
    uint32_t cell1_hour_start_seconds;   /* Cell1 OV 1小时窗口起始秒数 */
    uint32_t cell2_hour_start_seconds;   /* Cell2 OV 1小时窗口起始秒数 */
    uint32_t temp_hour_start_seconds;    /* 温度异常窗口起始秒数 */
} ExceptionCache_t;
```

**status_flags 位域布局**:

```
bit[0]:   cell1_tracking     — Cell1 OV 追踪中
bit[1]:   cell2_tracking     — Cell2 OV 追踪中
bit[2]:   temp_tracking      — 温度异常追踪中
bit[3-5]: temp_event_type    — 温度事件类型 (0=无, 2=过温, 3=低温)
bit[6-7]: charge_state       — 充放状态 (0=idle, 1=充电, 2=放电)
```

访问宏: `CACHE_GET_CELL1_TRACKING(cache)` / `CACHE_SET_CELL1_TRACKING(cache, v)` 等。

### 3.5 BatteryRecordStorage_t — Flash 数据镜像 (~110B)

```c
/* fml/g_data.h, 存放在 ap->record_storage (RAM 镜像) */
typedef struct {
    uint32_t magic;                 /* 魔数: MAGIC_VALUE_V6 = 0x42415436 */
    uint8_t  exception_counter;     /* 历史异常总数 0~255 */
    uint8_t  write_ptr;             /* 环形缓冲写指针 0~4 (MAX_RECORDS=5) */
    uint16_t padding1;
    BatteryExceptionRecord_t records[5]; /* 5 条记录, 各 20B = 100B */
    uint16_t checksum;              /* 加法校验和 */
} BatteryRecordStorage_t;
```

### 3.6 ProductInfo_t — 产品信息 (100B)

```c
/* app/product_info.h */
typedef struct {
    char manufacturer_name[20];   /* 制造商名称, ASCII null-terminated */
    char model_name[20];          /* 产品型号 */
    char battery_mfr[20];         /* 电芯制造商 */
    char battery_model[20];       /* 电芯型号 */
    char battery_prod_date[20];   /* 电芯生产日期 (如 "2026-01-15") */
} ProductInfo_t;                  /* 总计: 100 bytes */
```

### 3.7 gd_t 新增字段汇总

```c
/* fml/g_data.h → struct gd_t (热启动区，RAM @0x20000200) */

/* 循环计数相关 */
uint8_t  Battery_cycle_count;      /* 循环次数 0~255 */
uint8_t  Battery_charger_cnt;      /* 充电次数计数 (保留) */
uint16_t soc_accum_hundredths;     /* SOC 累积器 0~9999, 单位 0.01% */
uint8_t  soc_prev_dischg;          /* 上次放电 SOC 基线 (0xFF=未初始化) */
uint8_t  _bat_cycle_pad;           /* 对齐填充 */

/* SoH/Rdc */
uint8_t  Bat_Rdc;                  /* 内阻估算 (mΩ) */
int8_t   Bat_SoH;                  /* 健康度 0~100% */

/* RTC */
uint64_t Bat_RTC_Timer;            /* 旧字段，保留兼容 */
uint32_t Bat_RTC_Seconds;          /* 运行秒数 (CONFIG_NEW_CCC_LOG_ENABLE) */
uint16_t Bat_RTC_Milliseconds;     /* 毫秒精度 0~999 */

/* CV 偏移 (特性2写入，buckboost 读取) */
/* 注: cycle_cv_offset_mv 存在 gd 中，供 buckboost.c 读取 */
```

---

## 4. 特性详解

### 4.1 特性 1：电池循环充电次数自动统计

#### 功能描述

每 100ms 采集 `gd->real_soc_show`，仅在放电模式 (`BUCKBOOST_DISCHG_MODE`) 下累积 SOC 下降量。累积值满 100.00%（即 `CYCLE_SOC_UNIT_HUNDREDTHS = 10000`）时，`Battery_cycle_count++`，并标记 Flash 需要更新。

#### 触发条件

- `CONFIG_BAT_CYCLE_COUNT_ENABLE = 1`
- 当前模式为 `BUCKBOOST_DISCHG_MODE`（g_buckboost.woke_mode 判断）
- `real_soc_show` 数值有效（0~100）

#### 核心算法详解

```
bat_cycle_update() — 100ms 轮询
─────────────────────────────────────────────────────
  soc_now = gd->real_soc_show (0~100整数)

  if DISCHG_MODE:
    if soc_prev_dischg == 0xFF:       → 首次进放电，初始化基线
      soc_prev_dischg = soc_now
    elif soc_now < soc_prev_dischg:   → SOC 有下降
      delta = (soc_prev_dischg - soc_now) * 100   (单位: 0.01%)
      delta = min(delta, CYCLE_SOC_DELTA_MAX=500)  (防毛刺: 单步≤5%)
      soc_accum_hundredths += delta
      soc_prev_dischg = soc_now        (更新基线)
      while soc_accum_hundredths >= 10000:
        soc_accum_hundredths -= 10000
        Battery_cycle_count++          (最大 255)
        bat_cycle_mark_dirty()         (设 s_bat_cycle_dirty=1)
    else:                             → SOC 未下降（回升/持平）
      soc_prev_dischg = soc_now        (更新基线，防零漂积累)
  else (非放电模式):
    soc_prev_dischg = soc_now          (保持基线同步)
```

**防毛刺机制**: `CYCLE_SOC_DELTA_MAX = 500`（5.00%）限制单个 100ms tick 的最大贡献，防止 SOC 传感器跳变造成虚计。

**精度分析**:
- 分辨率: 0.01% SOC
- 最小计数单位: SOC 累积满 100%
- 整数 SOC（0~100）×100 = 0.01% 精度

#### 关键参数

| 参数 | 值 | 含义 |
|------|----|------|
| `CYCLE_SOC_UNIT_HUNDREDTHS` | 10000 | 计满一次需 100.00% SOC delta |
| `CYCLE_SOC_DELTA_MAX` | 500 | 单步最大 5% 防毛刺 |
| `ADDR_CYCLE_COUNT_PAGE` | 0x1A00 | Flash 存储页地址 |

#### 调用时序图

```
冷启动
  gd_data_init()
    └── bat_cycle_init()
          └── 读 Flash 0x1A00 → CycleCountStorage_t
              校验 checksum+checksum_inv
              若有效: 恢复 cycle_count / soc_prev_dischg / soc_accum_hundredths
              若无效: 全部置0 (soc_prev_dischg=0xFF)

APL_TASK 100ms (APL_EVT_GAUGE)
  fml_task_event_handler()
    └── Cyclic()                          ← BMS 算法更新 real_soc_show
    └── bat_cycle_update()                ← 本模块主逻辑
          └── (如计数++) bat_cycle_mark_dirty()

进睡眠 (SLP_vNormalToSleep)
  └── bat_cycle_force_save()
        └── 若 s_bat_cycle_dirty:
              hal_fmc_erase_page(0x1A00)
              bat_cycle_write_flash(&s)
              s_bat_cycle_dirty = 0

工程模式退出
  usb_bridge.c
    └── bat_cycle_mark_dirty()
    └── bat_cycle_force_save()
```

#### Edge Cases

| 场景 | 处理方式 |
|------|---------|
| 冷启动 Flash 全 0xFF | checksum_inv=0xFF≠~0x00，识别为无效，全部归零 |
| SOC 毛刺跳变 >5% | delta 被 clamp 到 500 (5%) |
| Battery_cycle_count = 255 | 不再递增，保持上限 |
| 热启动（进出睡眠） | 从 RAM gd→ 直接读取，不重读 Flash |
| 工程模式注入 SOC | `real_soc_show` 使用注入值，正常路径照常计算 |

#### 调试 Log

```
[BAT] cycle count: 42        ← 每次 count++ 时打印
```

---

### 4.2 特性 2：循环次数驱动的 CV 降压分级

#### 功能描述

随着电池老化（循环次数增加），降低充电截止电压 CV，延缓电芯进一步损伤，延长实际使用寿命。

#### 触发条件

- `CONFIG_CYCLE_CV_REDUCTION_ENABLE = 1`
- `BUCKBOOST_USED_NU6805 = 1`
- APL_EVT_GAUGE（100ms 定时）

#### 核心算法

```c
/* fml/_fml.c, APL_EVT_GAUGE 中 */
uint16_t cv_offset_mv = 0;
if (Battery_cycle_count >= CYCLE_CV_TIER3_COUNT)      // ≥ 210
    cv_offset_mv = CYCLE_CV_TIER3_OFFSET;             // 400 mV
else if (Battery_cycle_count >= CYCLE_CV_TIER2_COUNT) // ≥ 140
    cv_offset_mv = CYCLE_CV_TIER2_OFFSET;             // 300 mV
else if (Battery_cycle_count >= CYCLE_CV_TIER1_COUNT) // ≥ 70
    cv_offset_mv = CYCLE_CV_TIER1_OFFSET;             // 200 mV

gd->cycle_cv_offset_mv = cv_offset_mv;
/* buckboost.c 在设置充电 CV 时读取:
   actual_cv = BATTERY_CV_VALUE - gd->cycle_cv_offset_mv */
```

**分级示意**（2S 双节电池，`CONFIG_BATTERY_CELL_COUNT = 2`）:

| 循环次数 | CV 偏移 | 单节 CV (mV) | 总包 CV 发给 NU6805 (mV) |
|---------|--------|-------------|------------------------|
| 0 ~ 69  | 0      | 4400        | **8800**               |
| 70~139  | 200    | 4200        | **8400**               |
| 140~209 | 300    | 4100        | **8200**               |
| ≥ 210   | 400    | 4000        | **8000**               |

> 代码实现: `adjusted_cv_pack = (BATTERY_CV_VALUE - cv_offset_mv) * CONFIG_BATTERY_CELL_COUNT`
> 其中 `BATTERY_CV_VALUE = 4400`（单节基准 CV），`CONFIG_BATTERY_CELL_COUNT = 2`

#### 关键参数

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `BATTERY_CV_VALUE` | 4400 mV | 基准 CV |
| `CYCLE_CV_TIER1_COUNT` | 70 | 第1级阈值 |
| `CYCLE_CV_TIER1_OFFSET` | 200 mV | 第1级降压量 |
| `CYCLE_CV_TIER2_COUNT` | 140 | 第2级阈值 |
| `CYCLE_CV_TIER2_OFFSET` | 300 mV | 第2级降压量 |
| `CYCLE_CV_TIER3_COUNT` | 210 | 第3级阈值 |
| `CYCLE_CV_TIER3_OFFSET` | 400 mV | 第3级降压量 |

#### 调用时序

```
APL_EVT_GAUGE (100ms)
  └── _fml.c CV 计算块
        └── gd->cycle_cv_offset_mv = cv_offset_mv

充电参数设置（_fml.c, 同一 APL_EVT_GAUGE 块内）
  └── adjusted_cv_pack = (BATTERY_CV_VALUE - cv_offset_mv) * CONFIG_BATTERY_CELL_COUNT
  └── hal_nu6805_buckboost_charge_target_volt(adjusted_cv_pack)
       → 实际下发给 NU6805 的是总包 CV（8800/8400/8200/8000 mV）
```

#### 注意事项

- `gd->cycle_cv_offset_mv` 在 gd_t 中声明，需确认热启动区范围包含此字段
- 修改阈值时，需同步更新外部客户文档中的"维护建议"

---

### 4.3 特性 3：SoH / Rdc 估算

#### 功能描述

基于循环次数的线性模型，估算电池健康度 SoH (State of Health) 和内阻 Rdc，供 USB Bridge 遥测上报。

#### 核心算法

```c
/* fml/_fml.c, APL_EVT_GAUGE 中, 每 100ms 更新 */
if (gd->Battery_cycle_count <= 50) {
    gd->Bat_Rdc = P_R0Dsg_mOhm[0];    /* 基准内阻, 来自 MATLAB gauge 参数 */
    gd->Bat_SoH = 100;
} else {
    /* 线性衰退模型:
     * SoH: 每多1次循环(超过50次)减少 0.05%
     *      SoH = 100 - (cycle - 50) * 0.05
     * Rdc: 每多1次循环增加 0.05% 的基准内阻
     *      Rdc = R0 + R0 * (cycle - 50) * 5 / 10000
     */
    gd->Bat_Rdc = P_R0Dsg_mOhm[0]
                  + P_R0Dsg_mOhm[0] * (gd->Battery_cycle_count - 50) * 5 / 10000;
    gd->Bat_SoH = 100 - (gd->Battery_cycle_count - 50) * 5 / 100;
    if (gd->Bat_SoH < 0) gd->Bat_SoH = 0;
}
```

**模型参数**:

| 循环次数 | SoH | Rdc 增量 |
|----------|-----|---------|
| ≤ 50     | 100% | 0 |
| 100      | 97.5% | +0.25% |
| 200      | 92.5% | +0.75% |
| 255      | 89.75% | +1.025% |

> **P_R0Dsg_mOhm[0]**: 由 MATLAB Simulink gauge 模型生成，通常在 `gauge/` 目录下，值为电池在放电状态下的基准内阻（mΩ）。修改前需确认 gauge 参数已针对本项目电芯标定。

#### 数据类型注意

- `Bat_SoH`: `int8_t`（-128~127），最大值 100，最小值 clamp 为 0
- `Bat_Rdc`: `uint8_t`（0~255 mΩ），若实际内阻 > 255mΩ 会溢出，需评估实际电芯

#### 调试方法

串口打印（需手动加调试 log）或通过 USB Bridge 工程模式读取上位机显示值。

---

### 4.4 特性 4：异常日志 (bat_record) — OV/OT 记录

#### 功能描述

检测电池过压 (OV) 和温度异常 (OT/UT)，以 1 小时去抖动窗口策略写入 Flash，最多保存 5 条记录（环形覆盖）。

#### 触发条件

**OV 触发**:
- 单节电压（经 dyn_imp IR 补偿后）> `OVER_VOLTAGE_THRESHOLD = 4450 mV`
- 可使用工程模式注入虚拟电压触发

**OT 触发**:
- NTC 温度 > `CHRG_NTC_OT_TEMP_VALUE = 600`（即 60.0°C）

#### 1 小时窗口策略

```
OV 首次触发:
  ┌─────────────────────────────────────────────────┐
  │ 立即写 Flash（记录首次触发时刻和电压）           │
  │ 记录 cell1_hour_start_seconds = Bat_RTC_Seconds  │
  │ CACHE_SET_CELL1_TRACKING(cache, 1)               │
  │ 更新 cell1_max_voltage                           │
  └─────────────────────────────────────────────────┘

OV 持续中（1小时窗口内）:
  ┌─────────────────────────────────────────────────┐
  │ 只更新 RAM 最大值 cell1_max_voltage              │
  │ 不写 Flash                                       │
  └─────────────────────────────────────────────────┘

1小时窗口到期:
  ┌─────────────────────────────────────────────────┐
  │ 若最大值有更新: 写 Flash（更新最大电压记录）      │
  │ 重置窗口: cell1_hour_start_seconds = 当前秒数    │
  └─────────────────────────────────────────────────┘

OV 恢复（电压降到正常）:
  ┌─────────────────────────────────────────────────┐
  │ 若距首次触发 > 1小时 且 最大值有更新:            │
  │   写 Flash（最终记录）                           │
  │ 清除 CELL1_TRACKING 标志                         │
  └─────────────────────────────────────────────────┘
```

OT 采用相同窗口策略。

#### IR-drop 补偿集成

```c
/* bat_record.c, OV 检测前 */
#if (CONFIG_DYNAMIC_IMPEDANCE_ENABLE == 1)
    cell1_voltage = dyn_imp_compensate_cell1(cell1_voltage, ibat);
    cell2_voltage = dyn_imp_compensate_cell2(cell2_voltage, ibat);
#endif
/* 补偿后电压才与 4450mV 比较 */
```

#### 环形缓冲写入逻辑

```
write_ptr: 0~4 (MAX_RECORDS=5)
新记录写入 records[write_ptr]
write_ptr = (write_ptr + 1) % 5
exception_counter++
```

**注意**: 超过 5 条后，旧记录被覆盖（最新5条有效）。`exception_counter` 继续单调递增（上限 255），用于判断总历史异常次数。

#### 工程模式支持

```c
/* 工程模式注入后，bat_record 读取虚拟值 */
cell1_voltage = eng_mode_get_cell1();   /* 有注入则返回注入值 */
cell2_voltage = eng_mode_get_cell2();
temp_value    = eng_mode_get_temp();
```

#### 关键参数

| 参数 | 值 | 说明 |
|------|----|------|
| `OVER_VOLTAGE_THRESHOLD` | 4450 mV | 单节 OV 门限（CV=4400+50mV 余量）|
| `CHRG_NTC_OT_TEMP_VALUE` | 600 | 60.0°C 过温门限 |
| `MAX_RECORDS` | 5 | Flash 最大记录条数 |
| `ONE_HOUR_MS` | 3600000 UL | 1 小时毫秒数（用于 RTC 秒数比较时转换）|
| `MAGIC_VALUE_V6` | 0x42415436 | 'BAT6' 当前版本标识 |

#### 调用时序

```
冷启动:
  battery_record_init()
    ├── 读 Flash 0x1400 magic
    ├── magic == 'BAT6': 加载 record_storage 到 ap->record_storage
    └── magic != 'BAT6': 初始化空存储，写入魔数

APL_EVT_GAUGE (100ms):
  battery_record_periodic_check()
    ├── battery_record_update_overvoltage()
    └── battery_record_update_temperature()

读取记录 (USB Bridge 或工程模式):
  battery_record_read_exceptions(buf, max_count)
    └── 从 ap->record_storage.records[] 拷贝到 buf

工程模式擦除:
  battery_record_erase_all()
    ├── hal_fmc_erase_page(0x1400)
    └── 重置 ap->record_storage + 写空白头
```

#### 调试 Log

```
[BAT_REC] OV cell1: 4465mV (comp), total: 8920mV, record_id=3
[BAT_REC] OT temp: 612 (61.2C), state=CHG, record_id=4
[BAT_REC] Flash write record[2], write_ptr->3
```

（实际 log 内容取决于 bat_record.c 中 printk 实现）

---

### 4.5 特性 5：动态阻抗检测 (dyn_imp)

#### 功能描述

在 PD 充电会话开始时，通过差分电流法（1A→2A 步进）测量双节串联电池的正/负极引线阻抗，用于 bat_record OV 检测的 IR-drop 补偿，避免线阻虚报过压。

#### 触发条件

- `CONFIG_DYNAMIC_IMPEDANCE_ENABLE = 1`
- PD explicit_contract 已建立（确保充电电流可控）
- 当前模式为 `BUCKBOOST_CHAGER_MODE`
- `Vbat < 7500mV`（双节总压，约每节 3750mV，避免在高 SOC 充电时步进电流过大）
- 每次上电仅测一次（`s_done` 标志，重启复位）

#### 状态机（7 状态）

```
DYN_IDLE
    │  触发条件满足
    ▼
DYN_SETTLE_1A ──── 设 ibat_limit=1A
    │  等待 3×100ms = 300ms 稳定
    ▼
DYN_SAMPLE_1A ──── 采样 Vbat_total_1A 和 Vmid_1A
    │
    ▼
DYN_SETTLE_2A ──── 设 ibat_limit=2A
    │  等待 3×100ms = 300ms 稳定
    ▼
DYN_SAMPLE_2A ──── 采样 Vbat_total_2A 和 Vmid_2A
    │
    ▼
DYN_CALC
    │  ΔI = 1A
    │  R_pos = Vmid_1A - Vmid_2A         (mΩ，正极引线)
    │  R_neg = (Vbat_1A - Vbat_2A) - R_pos (mΩ，负极引线)
    │  若 R_pos/R_neg 在 [0, 500mΩ] 内: s_valid=true
    │  否则: s_valid=false (异常，不补偿)
    ▼
DYN_DONE
    │  s_done=true，本上电周期不再重测
    ▼  （保持此状态直至断电复位）
```

#### 差分电流法原理

```
物理模型 (大端 = 充电方向):
    VBUS ─── R_line ─── [ Cell1 ] ─── Vmid_point ─── [ Cell2 ] ─── GND
                │                           │                           │
              R_pos (正极线阻)           Vmid (ADC)                R_neg (负极线阻)

电流步进: 1A → 2A, ΔI = 1A

R_pos = (Vmid_1A - Vmid_2A) / ΔI = (Vmid_1A - Vmid_2A) mV  [因 ΔI=1A, R=ΔV/ΔI=ΔV(mΩ)]
R_total = (Vbat_1A - Vbat_2A) mV
R_neg = R_total - R_pos
```

#### 补偿公式

```c
/* 充电时 ibat > 0，补偿方向：真实电芯电压 = ADC电压 + IR压降 */
comp_cell1 = v_cell1_mv + (int32_t)i_bat_ma * s_r_pos / 1000;
comp_cell2 = v_cell2_mv + (int32_t)i_bat_ma * s_r_neg / 1000;
/* 例: R_pos=50mΩ, I=2000mA → 补偿量 = 2000*50/1000 = 100mV */
```

#### 有效性范围

| 参数 | 有效范围 | 超出处理 |
|------|---------|---------|
| R_pos | 0 ~ 500 mΩ | s_valid=false |
| R_neg | 0 ~ 500 mΩ | s_valid=false |

#### 调试查询接口

```c
dyn_imp_is_valid()           // 返回 bool: 测量是否有效
dyn_imp_get_r_pos_mohm()     // 返回 R_pos (mΩ)
dyn_imp_get_r_neg_mohm()     // 返回 R_neg (mΩ)
```

#### 注意事项

- 测量期间充电电流有 1A / 2A 两个短暂步进，持续约 600ms，对充电过程影响可忽略
- 若 PD 适配器不支持电流设置（如 5V 固定），`explicit_contract` 不成立，测量不触发
- 测量结果仅用于当前上电会话，断电后 R 值不持久化

#### 采样质量改进（v2）

为消除 ADC 噪声和开关电源纹波干扰，各采样点均取 4 次均值：

| 信号 | 采样方式 | 均值次数 |
|------|---------|---------|
| Vmid | `hal_badc_meas()` 直接 ADC | 4次 |
| Vbat | `hal_nu6805_buckboost_get_bat_voltage()` I2C | 4次 |
| Ibat | `hal_nu6805_buckboost_get_bat_current()` I2C | 4次 |

**阻抗计算改用实测 ΔI**（而非固定 1000mA）：

R_pos (mΩ) = (Vmid_1A − Vmid_2A) × 1000 / ΔI
R_neg (mΩ) = ((Vbat_1A − Vbat_2A) − (Vmid_1A − Vmid_2A)) × 1000 / ΔI

其中 ΔI = ibat_2A − ibat_1A（实测，期望 ~1000mA）

新增保护：`delta_i < 200mA` 时中止测量，防止小电流步进导致除法溢出或结果失真。

相关宏：`#define DYN_AVG_SAMPLES 4`

---

### 4.6 特性 6：RTC 时间戳

#### 功能描述

软件 RTC，以 APL_TASK 100ms 定时器累加实现秒级时间追踪，用于异常记录的时间戳标注。

#### 实现细节

```c
/* APL_TASK 100ms 定时器中 */
gd->Bat_RTC_Milliseconds += 100;
if (gd->Bat_RTC_Milliseconds >= 1000) {
    gd->Bat_RTC_Milliseconds -= 1000;
    gd->Bat_RTC_Seconds++;
}
```

**初始时间设置** (冷启动):

```c
/* fml/g_data.c, gd_data_init() 冷启动分支 */
#if CONFIG_RTC_USE_CUSTOM_TIME
Bat_RTC_Seconds = seconds_from_epoch(
    CONFIG_RTC_DEFAULT_YEAR,    /* 2026 */
    CONFIG_RTC_DEFAULT_MONTH,   /* 2 */
    CONFIG_RTC_DEFAULT_DAY,     /* 25 */
    CONFIG_RTC_DEFAULT_HOUR,    /* 0 */
    CONFIG_RTC_DEFAULT_MINUTE,  /* 0 */
    CONFIG_RTC_DEFAULT_SECOND   /* 0 */
);
#endif
```

#### 时间戳获取 — get_current_timestamp()

```c
/* bat_record.c 使用，原子读保护 */
VIC_vModuleDisable(...);                     // 关中断（原子保护）
uint32_t seconds = gd->Bat_RTC_Seconds;
VIC_vModuleEnable(...);
/* 将 seconds 转换为 TimeStamp_t (年月日时分秒) */
```

**精度**: 100ms 级（受 APL_TASK 调度精度影响）

**范围**: uint32_t 秒数，从 2026-02-25 起计，约 136 年溢出

#### TimeStamp_t 转换说明

从 `Bat_RTC_Seconds` 转回年月日需考虑闰年和月份天数。CK802 无硬件 RTC，需纯软件实现，约 20 行代码。

#### 热启动一致性

热启动（进出睡眠）时，`Bat_RTC_Seconds` 保留在 RAM，不重新初始化，因此时间连续。冷启动（断电后）从配置时间重新开始，这是设计取舍（无硬件 RTC 备电）。

---

### 4.7 特性 7：ProductInfo 产品信息持久化

#### 功能描述

将制造商、产品型号、电芯信息等 5 个字段（各 20 字节）持久化到 Flash 0x1800，供 USB Bridge 读取后上报 Windows 上位机。

#### 接口

```c
/* app/product_info.h */
void product_info_read(ProductInfo_t *info);   // 从 Flash 读取到 info
void product_info_write(const ProductInfo_t *info); // 擦页后写入 Flash
void product_info_print(void);                 // 启动时打印到串口
```

#### Flash 写入注意事项（Big-endian 兼容）

```
CK802 大端序写入 Flash:
- 每次 hal_fmc_write_word(addr, val) 写 4 字节
- 字符串 "ARUN" 在 Flash 中存储为: 'A'(MSB) 'R' 'U' 'N'(LSB)
- 与 little-endian PC 读取时字节序不同，PC 端无需反转（按字节读即正确）
```

#### USB 写入流程

```
Windows PC
  └── CMD_WRITE_REGISTER → WB7720 (I2C 0x42)
        └── i2c_buff[PRODUCT_INFO 区域] 更新
              └── NU17112 usb_bridge.c 检测变化
                    └── product_info_write(&info_from_i2c)
                          ├── hal_fmc_erase_page(0x1800)
                          └── 逐字 word 写入
```

#### 调试 Log

`product_info_print()` 在启动时执行，串口输出：

```
[PROD] manufacturer: ARUN
[PROD] model: IP162N
[PROD] battery_mfr: ATL
[PROD] battery_model: LP-5000-2S
[PROD] battery_prod_date: 2026-01-15
```

---

### 4.8 特性 8：USB 监测子系统（三击激活 + 遥测上报）

#### 子系统总览

USB 监测子系统由三个部分协同工作，形成完整的数据采集→传输→展示链路：

```
NU17112（主控，I2C Master）
   │  写 i2c_buff[256] 共享内存
   │  I2C @ 0x42
   ▼
WB7720（USB Bridge 芯片，I2C Slave）
   │  USB HID 协议，64 字节/包，1Hz 刷新
   ▼
Windows PC（HID Host）
   └── 配套上位机软件实时展示
```

- **NU17112**：采集所有新国标数据（SOC/V/I/T/SoH/Rdc/循环次数/异常日志），通过 I2C 写入 WB7720 的共享内存 `i2c_buff[256]`
- **WB7720**：独立 USB Bridge 芯片，内部维护 `i2c_buff[256]` 共享内存作为双端口 RAM。从 I2C 侧接收数据，组装为 64 字节 HID 报告，以 1Hz 频率推送给 PC
- **Windows PC**：运行配套上位机软件，接收 HID 报告，实时展示电池仪表盘、异常日志、设备信息三个界面

**三种 HID 数据包类型**（由 i2c_buff 中 CMD 字段控制 round-robin 切换）：

| Type | 内容 | 刷新策略 |
|------|------|---------|
| `0x01` | 实时遥测（SOC / Vbat / Ibat / T / SoH / Rdc / 循环次数）| 每秒持续推送 |
| `0x02` | 异常日志（OV/OT/UT，最多 5 条，3 页循环）| round-robin |
| `0x03` | 设备信息（ProductInfo 3 子页：制造商/型号/电芯）| CMD 触发 |

#### 激活入口：三击按键

USB_COM 监测模式需手动激活，防止日常使用误进入：

```
连续三击电源按键（led.c 检测）
  │
  ├─ usb_comm_activated = 1
  ├─ usb_comm_lock()            → 端口管理器切换为 PORT_STATE_NONE
  │    └─ pdlib_restart_typec(PORT0)  → 踢回 TypeC 状态机，触发重新协商
  ├─ LED5 闪 3 次               → 进入确认反馈
  └─ （开始等待 CC 检测，见下节）

再次三击或按任意键退出：
  ├─ usb_comm_activated = 0
  ├─ usb_comm_unlock()          → 恢复端口仲裁
  ├─ ubsd_wb7720_sleep()        → 关闭 WB7720
  └─ LED5 闪 1 次               → 退出确认反馈
```

#### FORCE_SINK 机制（手机供 VBUS 给 WB7720）

WB7720 需要 VBUS 才能枚举为 USB HID 设备。USB_COM 模式下充电宝切换为 TypeC SINK 角色，由接入的手机或 PC 通过 TypeC-A 口提供 VBUS 电源。

NU17112 的 TypeC 状态机默认运行 DRP（双向检测），会周期性尝试 SRC 角色。为确保在 USB_COM 激活期间始终保持 SINK，必须在状态机的三个关键入口同时拦截：

| 守卫位置 | 文件 | 拦截条件 | 动作 |
|---------|------|---------|------|
| `TC_SRC_Unattached_Entry` | `libpowerbank/typec.c` | `usb_comm_activated && PORT0` | 重定向 → `TC_SNK_Unattached` |
| `TC_Try_SRC_Entry` | `libpowerbank/typec.c` | `usb_comm_activated && PORT0` | 重定向 → `TC_SNK_Unattached` |
| `TC_SRC_Attached_Entry` | `libpowerbank/typec.c` | `usb_comm_activated && PORT0` | 重定向 → `TC_SNK_Unattached` |

三道守卫确保 TypeC-A 口 CC 线保持 Rd（SINK），无论 DRP 状态机处于哪个子状态。配合 `pdlib_restart_typec()` 的初始踢出，TypeC-A 口可在几秒内稳定进入 SINK 角色并完成连接握手。

#### CC 状态驱动 WB7720 生命周期

激活后，`fml/_fml.c` 每 100ms 轮询 CC 状态，自动管理 WB7720 的开关，实现无感知热插拔：

```
USB_COM 激活中，每 100ms（APL_TASK 定时器）：
  │
  ├── hal_tcpc_get_cc(PORT0) 读取 CC 状态
  │
  ├── CC 有效（手机已插入，VBUS 建立）：
  │     ├── [WB7720 未唤醒] → ubsd_wb7720_wakeup()
  │     │                      + 立即推送 ProductInfo (Type 0x03 subpage 0~2)
  │     └── [WB7720 已唤醒] → 继续正常遥测推送（usb_bridge.c 填 i2c_buff）
  │
  └── CC 无效（未接入 / 手机已拔出）：
        └── ubsd_wb7720_sleep()   → WB7720 休眠，节省功耗
```

**设计优势**：WB7720 不长期保持运行。手机拔出后自动休眠，重新插入后自动唤醒，全程无需用户二次操作。

#### 数据推送路径（usb_bridge.c）

`app/usb_bridge.c` 负责 NU17112 侧的 I2C 数据搬运，每 100ms 执行一次：

```
APL_TASK 每 100ms 调用 usb_bridge_task()
  │
  ├── 写入 i2c_buff[遥测区（0x00~0x36）]：
  │     SOC / Vbat / Ibat / T / Battery_cycle_count / SoH / Rdc / ...
  │
  ├── 写入 i2c_buff[异常日志区（0x37~0x4D）]：
  │     bat_record.c 更新的 OV/OT/UT 记录（最多 5 条，各 20 字节）
  │
  └── 轮询 i2c_buff[CMD 区]，分发命令：
        CMD 0x01 → round-robin 切换下一数据包类型（Type 0x01/02/03）
        CMD 0x02 → 触发 Type 0x03 device info 子页读取
        CMD 0x0C → 工程模式写寄存器
```

详细寄存器布局见 `.claude/references/specs/WB7720_USB_Bridge_接口规范.md` (v1.3) 和 `USB三方数据格式对照表.md` (v1.3)。

#### LED 状态指示

| LED | 触发条件 | 状态 | 含义 |
|-----|---------|------|------|
| LED5 | 进入 USB_COM 模式时 | 快闪 3 次 | 激活确认 |
| LED5 | USB_COM 运行中 | 慢闪 | 监测模式进行中 |
| LED5 | 退出 USB_COM 模式时 | 闪 1 次 | 退出确认 |
| LED4 | WB7720 已唤醒 | 常亮 | USB 数据传输中 |
| LED4 | WB7720 休眠中 | 慢闪 | 等待手机 / PC 插入 |

#### 系统互斥（USB_COM 激活期间）

USB_COM 模式激活期间，以下模块被屏蔽，避免干扰数据通信或意外断电：

| 模块 | 被屏蔽的行为 | 原因 |
|------|------------|------|
| `sleep.c` | 禁止进入低功耗睡眠 | 睡眠会断开 I2C 和 VBUS |
| `buckboost.c` | 跳过充放电保护轮询 | 防止保护触发切断电源 |
| `port_manager.c` | 跳过端口枚举和充放电管理 | 避免端口状态被覆盖 |
| `wpc_idle.c` | 跳过 Qi 空闲处理 | 防止 WPC 与 USB_COM 的 DPDM MUX 冲突 |
| DPDM | MUX 切至 WB7720，停止 QC 协议引擎 | DPDM 线路由 WB7720 接管 |

#### 关键配置宏

| 宏 | 值 | 说明 |
|----|---|------|
| `CONFIG_USB_BRIDGE_ENABLE` | 1 | USB Bridge 子系统总开关 |
| `CONFIG_TRIPLE_CLICK_COMM_ENABLE` | 1 | 三击激活模式（0=自动模式，无需按键，CC 检测到即激活）|
| `CONFIG_USB_COM_FORCE_SINK` | 1 | 启用 FORCE_SINK 三道守卫 |
| `CONFIG_USB_COMM_LED5_BLINK` | 1 | LED5 模式指示 |
| `CONFIG_USB_COMM_LED4_WB_STATE` | 1 | LED4 WB7720 状态指示 |

#### 调试 Log

```
USB comm activated by triple-click    ← 进入 USB_COM 模式
[SLEEP] blocked by USB_COM mode       ← 休眠被阻断
[CC] CC valid, waking WB7720          ← 检测到手机插入，唤醒 WB7720
[CC] CC invalid, sleeping WB7720      ← 手机拔出，WB7720 休眠
USB comm exit by key N                ← 按键 N 退出
USB comm deactivated by triple-click  ← 三击退出
```

#### Edge Cases

| 场景 | 处理 |
|------|------|
| USB_COM 中电量耗尽 | `buckboost_protection_handle` 被跳过，不会强制关机，需用户手动退出 |
| USB_COM 中 WPC 来包 | `wpc_idle_phase_process` 直接返回，Qi 停止响应 |
| 三击再次触发 | `usb_comm_activated ^= 1`，切换为退出流程 |
| `buckboost_protection_flag` 已置位 | 三击无效（led.c 中有保护判断） |
| 手机拔出后重插 | CC 轮询自动触发 WB7720 re-wakeup，无需重新三击 |

---

## 5. 特性间依赖关系

### 5.1 依赖矩阵

|  | 特1:循环计数 | 特2:CV降压 | 特3:SoH/Rdc | 特4:bat_record | 特5:dyn_imp | 特6:RTC | 特7:ProductInfo |
|--|-------------|-----------|-------------|---------------|-------------|---------|-----------------|
| **特1:循环计数** | — | 提供数据 | 提供数据 | 无依赖 | 无依赖 | 无依赖 | 无依赖 |
| **特2:CV降压** | 依赖 | — | 无依赖 | 无依赖 | 无依赖 | 无依赖 | 无依赖 |
| **特3:SoH/Rdc** | 依赖 | 无依赖 | — | 无依赖 | 无依赖 | 无依赖 | 无依赖 |
| **特4:bat_record** | 无依赖 | 无依赖 | 无依赖 | — | 可选补偿 | 依赖时间戳 | 无依赖 |
| **特5:dyn_imp** | 无依赖 | 无依赖 | 无依赖 | 提供补偿 | — | 无依赖 | 无依赖 |
| **特6:RTC** | 无依赖 | 无依赖 | 无依赖 | 提供时间 | 无依赖 | — | 无依赖 |
| **特7:ProductInfo** | 无依赖 | 无依赖 | 无依赖 | 无依赖 | 无依赖 | 无依赖 | — |

### 5.2 典型数据流：dyn_imp → bat_record

```
充电开始 (PD explicit_contract 建立)
    │
    ▼
dyn_imp_update()  [100ms 轮询]
    │  状态机: IDLE→SETTLE_1A→SAMPLE_1A→SETTLE_2A→SAMPLE_2A→CALC→DONE
    │  约 700ms 完成测量
    │  结果: s_r_pos=45mΩ, s_r_neg=38mΩ, s_valid=true
    │
    ▼
APL_EVT_GAUGE 每 100ms:
  battery_record_periodic_check()
    └── battery_record_update_overvoltage()
          │
          ├── cell1_v = ADC读取 cell1 端电压 = 4420 mV
          ├── ibat = 2000 mA (充电中)
          │
          ├── [特5补偿] cell1_v = dyn_imp_compensate_cell1(4420, 2000)
          │           = 4420 + 2000*45/1000 = 4420 + 90 = 4510 mV
          │
          ├── 4510 mV > OVER_VOLTAGE_THRESHOLD(4450)?  → YES
          │
          ├── 首次触发:
          │     [特6时间戳] ts = get_current_timestamp()
          │     写 Flash 记录: {ts, OV, cell1, 4510mV, 8930mV, record_id++}
          │     CACHE_SET_CELL1_TRACKING = 1
          │     cell1_hour_start_seconds = Bat_RTC_Seconds
          │
          └── 后续 1小时内持续 OV: 只更新 cell1_max_voltage = max(4510, new_v)
```

---

## 6. 开启/关闭方法 — config.h 宏说明

### 6.1 完整宏列表

```c
/* app/config.h */

/*─── 全局门控 ─────────────────────────────────────────────*/
#define CONFIG_NEW_CCC_LOG_ENABLE       1   // 0: 关闭全部新国标特性
                                            // 影响: 特性 4/6/7 的代码块

/*─── 特性 1: 循环次数 ─────────────────────────────────────*/
#define CONFIG_BAT_CYCLE_COUNT_ENABLE   1   // 0: 关闭
#define CYCLE_SOC_UNIT_HUNDREDTHS       10000u  // 调大 = 计数门槛更高
#define CYCLE_SOC_DELTA_MAX             500u    // 调大 = 防毛刺宽度增大

/*─── 特性 2: CV 降压分级 ──────────────────────────────────*/
#define CONFIG_CYCLE_CV_REDUCTION_ENABLE  1 // 0: 关闭
#define CYCLE_CV_TIER1_COUNT              70    // 次
#define CYCLE_CV_TIER1_OFFSET             200   // mV
#define CYCLE_CV_TIER2_COUNT              140   // 次
#define CYCLE_CV_TIER2_OFFSET             300   // mV
#define CYCLE_CV_TIER3_COUNT              210   // 次
#define CYCLE_CV_TIER3_OFFSET             400   // mV
#define BATTERY_CV_VALUE                  4400  // mV 基准 CV

/*─── 特性 4: OV/OT 阈值 ──────────────────────────────────*/
#define OVER_VOLTAGE_THRESHOLD          4450    // mV 单节 OV 门限
#define CHRG_NTC_OT_TEMP_VALUE          600     // 0.1°C (60.0°C)

/*─── 特性 5: 动态阻抗 ────────────────────────────────────*/
#define CONFIG_DYNAMIC_IMPEDANCE_ENABLE 1       // 0: 关闭

/*─── 特性 6: RTC 初始时间 ───────────────────────────────*/
#define CONFIG_RTC_USE_CUSTOM_TIME      1       // 0: 使用默认 epoch
#define CONFIG_RTC_DEFAULT_YEAR         2026
#define CONFIG_RTC_DEFAULT_MONTH        2
#define CONFIG_RTC_DEFAULT_DAY          25
#define CONFIG_RTC_DEFAULT_HOUR         0
#define CONFIG_RTC_DEFAULT_MINUTE       0
#define CONFIG_RTC_DEFAULT_SECOND       0

/*─── USB Bridge ─────────────────────────────────────────*/
#define CONFIG_USB_BRIDGE_ENABLE        1
#define CONFIG_BATTERY_CAPACITY_MAH     5000    // mAh 标称容量
#define CONFIG_BATTERY_CELL_COUNT       2       // 电池节数
```

### 6.2 最小化开启（仅异常日志）

```c
#define CONFIG_NEW_CCC_LOG_ENABLE       1   // 必须
#define CONFIG_BAT_CYCLE_COUNT_ENABLE   0   // 关闭
#define CONFIG_CYCLE_CV_REDUCTION_ENABLE 0  // 关闭
#define CONFIG_DYNAMIC_IMPEDANCE_ENABLE 0   // 关闭
/* bat_record 仍可工作，时间戳精度降为冷启动起计秒数 */
```

### 6.3 关闭所有新国标特性

```c
#define CONFIG_NEW_CCC_LOG_ENABLE       0
#define CONFIG_BAT_CYCLE_COUNT_ENABLE   0
#define CONFIG_CYCLE_CV_REDUCTION_ENABLE 0
#define CONFIG_DYNAMIC_IMPEDANCE_ENABLE 0
```

---

## 7. 测试与验证

### 7.1 串口 Log 期望输出

**冷启动**（Flash 全 0xFF，第一次运行）:

```
[BAT_CYCLE] Flash empty, init cycle_count=0
[PROD] manufacturer: (empty)
[BAT_REC] Flash magic mismatch, init storage
[DYN_IMP] Waiting for PD explicit_contract...
```

**正常充电中（PD 已建立）**:

```
[DYN_IMP] Start measurement (Vbat=7100mV)
[DYN_IMP] Settle 1A... (3 ticks)
[DYN_IMP] Sample 1A: Vbat=7180mV, Vmid=3590mV
[DYN_IMP] Settle 2A... (3 ticks)
[DYN_IMP] Sample 2A: Vbat=7100mV, Vmid=3530mV
[DYN_IMP] R_pos=60mOhm, R_neg=20mOhm, valid=1
```

**OV 首次触发**:

```
[BAT_REC] OV cell1: raw=4420mV, comp=4510mV (R=60mOhm, I=1500mA)
[BAT_REC] Write record[0]: OV, cell1, max=4510mV, total=9010mV, id=1
[BAT_REC] 1-hour window start: 3600s
```

**循环次数增加**:

```
[BAT] cycle count: 1
[BAT] cycle count: 2
```

**进睡眠**:

```
[BAT_CYCLE] Force save: cycle=5, soc_accum=2300
[BAT_CYCLE] Flash write OK @ 0x1A00
```

### 7.2 工程模式注入方法

工程模式下可注入虚拟参数触发各检测逻辑：

| 注入参数 | 触发效果 | Windows 上位机操作 |
|----------|---------|------------------|
| cell1_voltage = 4500 mV | 触发 OV 记录 | Write 0x4500 到 cell1_voltage 寄存器 |
| temp = 650 (0.1°C) | 触发 OT 记录 | Write 0x0290 到 temp 寄存器 |
| cycle_count = 80 | 验证 CV 降压第1级 | Write 0x50 到 cycle_count 寄存器 |
| 调用 erase_all CMD | 清空所有日志 | 工程模式 CMD 0xE0 |

注入后触发 `battery_record_reset_tracking()` 清除 tracking 缓存，让新参数立即生效。

### 7.3 Flash 验证

使用 CDS 调试器或 J-Link 直接读取 Flash：

```bash
# 读取 CycleCount 页 (前 8B)
mem8 0x1A00 8
# 期望: cycle_count soc_prev soc_accum_H soc_accum_L checksum checksum_inv 0x00 0x00

# 读取 bat_record 页头 (前 16B)
mem8 0x1400 16
# 期望: 42 41 54 36  (magic='BAT6')
#        00 00 00 05  (exception_counter=5 例)
#        00 00 00 02  (write_ptr=2 例)
```

---

## 8. 已知限制与注意事项

### 8.1 RTC 精度限制

软件 RTC 依赖 APL_TASK 100ms 定时器，实际精度约 ±100ms/秒（OSAL 协作式调度非实时），长时间运行累计误差可能达到分钟级。无外部时钟校准手段。

**影响**: 异常记录时间戳仅供参考，不满足精确计时要求。

### 8.2 循环次数上限 255

`Battery_cycle_count` 类型为 `uint8_t`，最大 255。超出后计数停止，Flash 保存值为 255。若需更大范围，需将字段改为 `uint16_t`（同时更新 `CycleCountStorage_t`，需兼容旧 Flash 页格式）。

### 8.3 异常记录 5 条上限

`MAX_RECORDS = 5`，超出后环形覆盖最旧记录。`exception_counter` 继续单调递增记录总历史数。若需更多记录，可增大 `MAX_RECORDS`，但受 512B Flash 页限制（`5 × 20B = 100B + 16B头 = 116B`，当前页内最大可支持 `(512-16)/20 = 24` 条）。

### 8.4 Flash 写次数（寿命）

NU17112 Flash 典型擦写寿命约 10,000 次。

| 操作 | 触发时机 | 预估频率 |
|------|---------|---------|
| CycleCount 页写 | 每次进睡眠 | 每次放电结束，约每天1~3次 |
| bat_record 页写 | OV/OT 首次及每小时触发 | 异常时，正常使用极少 |
| ProductInfo 页写 | 工厂写入一次 | 极少 |

正常使用下 Flash 寿命无虞。但如果设备频繁短时进出睡眠，应确认 CycleCount 页写入频率不超过设计限制。

### 8.5 dyn_imp 仅每次上电测一次

重启后重新测量。适配器更换、线缆更换不会触发重测（直到下次上电）。如需强制重测，可在 `dyn_imp.c` 中添加 `dyn_imp_reset()` 接口（清除 `s_done` 标志）。

### 8.6 Big-endian Flash 兼容性

CK802 为大端序，Flash 中多字节整数以大端格式存储。PC 端（little-endian）读取 Flash 转储时需注意字节序，特别是 `record_id (uint32_t)` 和 `timestamp.year (uint16_t)` 等字段。

### 8.7 CV 降压与电池容量的平衡

实际 CV 降压后，充电容量会减少（CV 从 4400mV 降到 4000mV，约减少 5~10% 可用容量），这是设计意图（用容量换寿命）。需在产品规格书中注明"老化后容量可能略有下降"，避免用户投诉。

---

*文档版本: v1.0 | 生成时间: 2026-03-08*
